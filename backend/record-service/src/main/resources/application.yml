spring:
  application:
    name: record-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/hospitalDB

server:
  port: 8083

jwt:
  secret: ${JWT_SECRET:dev-secret-key-must-be-at-least-32-bytes!!}
